{
  "name": "hubot-munchie",
  "version": "1.0.0",
  "author": "jmccance",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "aws-sdk": "^2.205.0",
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@types/hubot": "^2.19.0",
    "@types/node": "^9.4.6",
    "hubot": "^2.19.0",
    "typescript": "^2.7.2"
  },
  "scripts": {
    "build": "yarn run tsc",
    "bot": "yarn run build && yarn run hubot -n b",
    "prepublish": "yarn build"
  }
}
